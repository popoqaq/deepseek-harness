/** Host half of the MCP settings UI package; the management service owns Host state. */

/** Deliberately empty Host plugin body for the browser-only contribution. */
export function apply(): void {}
